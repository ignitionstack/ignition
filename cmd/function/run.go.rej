@@ -104,7 +104,10 @@
 					return err
 				}
 
-				finalModel := m.(spinner.Model)
+				finalModel, ok := m.(spinner.Model)
+				if \!ok {
+					return fmt.Errorf("unexpected model type returned from spinner")
+				}
 				if finalModel.HasError() {
 					err := finalModel.GetError()
 					return err
